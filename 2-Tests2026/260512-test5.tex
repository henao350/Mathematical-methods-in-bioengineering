\documentclass[11pt,svgnames,spanish,oneside]{amsart}

\usepackage{hyperref}
%\hypersetup{colorlinks}
\usepackage{hypcap}
\usepackage{graphicx}
\usepackage{transparent}
\usepackage{amsmath,amsfonts,amssymb,amscd}
\usepackage{bbm,wasysym,stmaryrd,color,colortbl}
\usepackage{multimedia}
\usepackage{cancel}
\usepackage[normalem]{ulem}
\usepackage{mathrsfs}
%\usepackage{enumerate}
\usepackage{enumitem}
% \usepackage[spanish]{babel}
\usepackage{amsthm}
\usepackage{tikz}
\usepackage{pgfplots}
  \pgfplotsset{compat=1.18}
\usepackage[top=2.0cm,bottom=2.0cm,left=2.0cm,right=2.0cm,headsep=10pt]{geometry}
\usepackage{eso-pic}
\usepackage{url}

% Les principaux ensembles
\newcommand\N{{\mathbb N}}
\newcommand\R{{\mathbb R}}
\newcommand\T{{\mathbb T}}
\newcommand\C{{\mathbb C}}
\newcommand\Q{{\mathbb Q}}
\newcommand\Z{{\mathbb Z}}
\newcommand\Pp{{\mathbb P}}
\newcommand\Ee{{\mathbb E}}
\newcommand{\indiq}{{\bf 1}}
\usepackage{framed}
\usepackage{xcolor}
% Default fixed font does not support bold face
\DeclareFixedFont{\ttb}{T1}{txtt}{bx}{n}{8} % for bold
\DeclareFixedFont{\ttm}{T1}{txtt}{m}{n}{8}  % for normal

\usepackage{fancyhdr}

\setlength{\parskip}{5pt}
\setlength{\parindent}{0pt}

%%%%%%%%%%      Document     %%%%%%%%%%%%%%%

% Para escribir las soluciones
\usepackage{versions}
% \includeversion{solution}
\excludeversion{solution}


\let\epsilon\varepsilon
\newenvironment{mysolution}{%
  \solution\color{blue}Sol:
}{%
  \endsolution
}

\newcommand{\dd}{\, \text{d}}


\begin{document}

\title{Métodos matemáticos en bioingeniería\\[0.2em]
Test \# 5
}
\date{12 May 2026}
\author{Duvan Henao}

%\date{\today}

%\pagestyle{plain}

\maketitle

\thispagestyle{fancy}
\lhead{\includegraphics[width=0.28\textwidth]{logo-UOH}}
\rhead{First semester, 2026}
\begin{center}
\end{center}

%\cfoot{}

\vspace{-2em}


\begin{enumerate}[label=\textbf{P\arabic*.},itemsep=1em]
  \item Find the formula of a cosine function with amplitude $3$ and period $\pi$. Sketch the plot of that function.

  \medskip
  \item Which of the following functions

  \vspace{-1em}
  \begin{center}
  \begin{tabular*}{\textwidth}{@{\extracolsep{\fill}} p{0.45\textwidth} p{0.45\textwidth}}
    \textbf{a)} $f(t) = \sin \left( \frac{\pi t}{3} - 1 \right)$ &
    \textbf{b)} $f(t) = \sin \left( \frac{(t-1)\cdot \pi}{3} \right)$ \\[1em]
    \textbf{c)} $f(t) = \cos \left( \frac{(t-1)\cdot \pi}{3} \right)$ &
    \textbf{d)} $f(t) = \sin \left( \frac{(t+1)\cdot \pi}{3} \right)$
  \end{tabular*}
  \end{center}
  \medskip
  corresponds to the plot in the figure?

  \begin{center}
    \begin{tikzpicture}
    \begin{axis}[
        axis lines = middle,
        xtick = {-2,-1, 1, 2, 3, 4, 5, 6, 7},
        ytick = {-1, 1},
        xlabel = {$t$},
        ylabel = {$y$},
        xmin = -2.2, xmax = 8,
        ymin = -1.2, ymax = 1.2,
        domain = -1:7,
        samples = 100,
        grid = both,
        grid style = {dashed, gray!30},
        height = 6cm, width = 12cm,
        thick
    ]

    % The function plot
    \addplot [blue, smooth, ultra thick] {sin(deg((x-1)*pi/3))};

    % Add the marks at the requested points
    \addplot[only marks, mark=*, red, nodes near coords={}]
        coordinates {(-0.5, {sin(deg((-0.5-1)*pi/3))}) (1,0) (2.5, {sin(deg((2.5-1)*pi/3))}) (4, 0) (5.5, {sin(deg((5.5-1)*pi/3))}) (7,0)};

    \end{axis}
    \end{tikzpicture}
  \end{center}

\end{enumerate}


\end{document}
