\documentclass[11pt,svgnames,spanish,oneside]{amsart}

\usepackage{hyperref}
%\hypersetup{colorlinks}
\usepackage{hypcap}
\usepackage{graphicx}
\usepackage{transparent}
\usepackage{amsmath,amsfonts,amssymb,amscd}
\usepackage{bbm,wasysym,stmaryrd,color,colortbl}
\usepackage{multimedia}
\usepackage{cancel}
\usepackage[normalem]{ulem}
\usepackage{mathrsfs}
%\usepackage{enumerate}
\usepackage{enumitem}
\usepackage[spanish]{babel}
\usepackage{amsthm}
\usepackage{tikz}
\usepackage{pgfplots}
\usepackage[top=2.0cm,bottom=2.0cm,left=2.0cm,right=2.0cm,headsep=10pt]{geometry}
\usepackage{eso-pic}
\usepackage{url}

% Les principaux ensembles
\newcommand\N{{\mathbb N}}
\newcommand\R{{\mathbb R}}
\newcommand\T{{\mathbb T}}
\newcommand\C{{\mathbb C}}
\newcommand\Q{{\mathbb Q}}
\newcommand\Z{{\mathbb Z}}
\newcommand\Pp{{\mathbb P}}
\newcommand\Ee{{\mathbb E}}
\newcommand{\indiq}{{\bf 1}}
\usepackage{framed}
\usepackage{xcolor}
% Default fixed font does not support bold face
\DeclareFixedFont{\ttb}{T1}{txtt}{bx}{n}{8} % for bold
\DeclareFixedFont{\ttm}{T1}{txtt}{m}{n}{8}  % for normal

\usepackage{fancyhdr}

\setlength{\parskip}{5pt}
\setlength{\parindent}{0pt}

%%%%%%%%%%      Document     %%%%%%%%%%%%%%%

% Para escribir las soluciones
\usepackage{versions}
% \includeversion{solution}
\excludeversion{solution}


\let\epsilon\varepsilon
\newenvironment{mysolution}{%
  \solution\color{blue}Sol:
}{%
  \endsolution
}

\newcommand{\dd}{\, \text{d}}


\begin{document}

\title{Métodos matemáticos en bioingeniería\\[0.2em]
Test \# 4
}
\date{5 May 2026}
\author{Duvan Henao}

%\date{\today}

%\pagestyle{plain}

\maketitle

\thispagestyle{fancy}
\lhead{\includegraphics[width=0.28\textwidth]{logo-UOH}}
\rhead{First semester, 2026}
\begin{center}
\end{center}

%\cfoot{}

\vspace{-4em}

A bacterial culture is grown in a fixed environment with an initial population of $N_0 = 500$ cells and an estimated carrying capacity of $K = 10000$ cells. The intrinsic growth rate ($r$) is of 0.4 per hour.

% It is observed that after $1$ hour ($t=1$), the population has increased to $2000$ cells. Assuming the population follows the logistic growth model, determine the time $t$ required for the population to reach $8000$ cells.

\medskip

\begin{enumerate}[label=\textbf{P\arabic*.},itemsep=1em]
  \item
%   Identify the constants $K$, $N_0$, and calculate the growth rate $r$ using the logistic equation:
%   \begin{align*}
%    \frac{d N}{d t}=rN\Big (1-\frac{N}{K}\Big ),\qquad N(0)=N_0,
%   \end{align*}
%   where the solution is given by:
%   \begin{align*}
%    N(t) = \frac{K}{1 + \left(\frac{K}{N_0} - 1\right)e^{-rt}}
%   \end{align*}
Using the logistic equation:
  \begin{align*}
   N(t) = \frac{K}{1 + \left(\frac{K}{N_0} - 1\right)e^{-rt}}
  \end{align*}
  find the population after $t=1$ hour.

  \item
  Taking the Python code provided as a starting point, find the value of $t$ when $N(t) = 8000$. Explain, line by line, how the code works.

  \item Suppose that in a different bacterial population, with $N_0=500$ cells and carrying capacity of $K=10000$ cells, the population after $1$ hour ($t=1$) has increased to $3000$ cells. Adapt the code in Python so as to identify the intrinsic growth rate $r$.
\end{enumerate}



\end{document}
